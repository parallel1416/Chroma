
#ifndef GLOBALVAR_H
#define GLOBALVAR_H
#include <qsize.h>
int bgmSound, toolSound, storySound;
int curScene, prevScene;
int colorStatus[5];
QSize btnSize, boxSize;

class globalvar
{
public:
    globalvar();
};

#endif // GLOBALVAR_H
