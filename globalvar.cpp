
#include "globalvar.h"

globalvar::globalvar()
{
    bgmSound=50, toolSound=50, storySound=50;
    curScene=0;
    btnSize.setHeight(60);
    btnSize.setWidth(120);
    boxSize.setHeight(50);
    boxSize.setWidth(50);
}

